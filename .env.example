# SERP Radio Production Backend - Environment Variables

# === AWS Configuration ===
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_DEFAULT_REGION=us-east-1

# === S3 Storage ===
S3_BUCKET=serp-radio-artifacts

# === Snowflake (for GSC data) ===
SNOWFLAKE_USER=your-username
SNOWFLAKE_PASSWORD=your-password
SNOWFLAKE_ACCOUNT=your-account.region
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=RAW
SNOWFLAKE_SCHEMA=GSC
SNOWFLAKE_ROLE=SEO_RO

# === SERP API Configuration ===
SERP_API_KEY=your-serp-api-key

# === S3 Configuration Files ===
RULES_S3=s3://serp-radio-config/metric_to_label.yaml
MOTIF_CATALOG_S3=s3://serp-radio-config/motifs_catalog.json

# === Momentum Analysis ===
TOKEN_BAR_WINDOW=4
MOMENTUM_POS_THRESHOLD=0.65
MOMENTUM_NEG_THRESHOLD=0.35

# === Audio Rendering ===
RENDER_MP3=0  # Set to 1 to enable MP3 rendering (requires fluidsynth + ffmpeg)

# === CORS Configuration ===
CORS_ORIGINS=http://localhost:3000,https://your-frontend.lovable.dev

# === Lovable Frontend URL (if applicable) ===
LOVABLE_FRONTEND_URL=https://your-project.lovable.dev

# === Server Configuration ===
PORT=8080